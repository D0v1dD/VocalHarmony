<?xml version="1.0" encoding="utf-8"?>
<!-- If needed, replace `tools:context` with your actual Fragment/Activity class,
     or remove it if not using Tools preview. -->

<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"

    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ui.VoiceQualityFragment">

    <!-- Toolbar pinned at the top -->
    <com.google.android.material.appbar.MaterialToolbar
        android:id="@+id/toolbar"
        android:layout_width="0dp"
        android:layout_height="?attr/actionBarSize"

        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"

        android:contentDescription="@string/app_toolbar_description"
        android:theme="@style/ThemeOverlay.MaterialComponents.Dark.ActionBar"
        tools:ignore="MissingConstraints" />

    <!-- Scrollable container for buttons, pinned below the Toolbar & above the SNR Bar -->
    <ScrollView
        android:id="@+id/button_scroll"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:fillViewport="true"
        android:contentDescription="@string/scroll_view_description"

        app:layout_constraintTop_toBottomOf="@+id/toolbar"
        app:layout_constraintBottom_toTopOf="@+id/snr_bar"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="8dp">

        <LinearLayout
            android:id="@+id/button_container"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:gravity="center_horizontal"
            android:padding="16dp">

            <!-- Example Buttons -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/button_record_baseline"
                style="@style/Widget.MaterialComponents.Button.OutlinedButton"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="@string/record_baseline_noise"
                android:layout_marginBottom="8dp" />

            <com.google.android.material.button.MaterialButton
                android:id="@+id/button_start_record"
                style="@style/Widget.MaterialComponents.Button.OutlinedButton"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="@string/start_recording"
                android:layout_marginBottom="8dp" />

            <!-- Add other buttons as needed... -->

        </LinearLayout>
    </ScrollView>

    <!-- SNR Bar pinned below the ScrollView, above the chart -->
    <com.example.vocalharmony.ui.home.SNRBar
        android:id="@+id/snr_bar"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:contentDescription="@string/snr_bar_description"

        app:layout_constraintTop_toBottomOf="@+id/button_scroll"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="12dp" />

    <!-- Audio Chart pinned to the bottom; occupies 40% height -->
    <com.github.mikephil.charting.charts.LineChart
        android:id="@+id/audio_chart"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:contentDescription="@string/audio_chart_description"

        app:layout_constraintTop_toBottomOf="@+id/snr_bar"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHeight_default="percent"
        app:layout_constraintHeight_percent="0.4" />

</androidx.constraintlayout.widget.ConstraintLayout>
